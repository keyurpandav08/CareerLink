# CareerLink Backend

This folder contains the Spring Boot backend for CareerLink.

Key areas:

- `controller` for HTTP endpoints
- `service` for business logic
- `repository` for data access
- `entity` and `dto` for persistence and API models
- `config` for security, bootstrapping, and app settings
