spring:
  application:
    name: jobberdb

  datasource:
    url: jdbc:postgresql://localhost:5432/jobberdb
    username: postgres
    password: admin

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true

  h2:
    console:
      enabled: true
      path: /h2-console

  mail:
    host: smtp.gmail.com
    port: 587
    username: try.keyurpandav@gmail.com
    password: zswuziksbfjyclme
    properties:
      mail:
        smtp:
          auth: true
          starttls:
            enable: true
          connectiontimeout: 5000
          timeout: 5000
          writetimeout: 5000

app:
  frontend:
    base-url: http://localhost:5173
  mail:
    from-name: JobLithic
    otp-expiry-minutes: 10

server:
  port: 8080
